# processes/ — verbs

Only movements that actually run. Three exist in this repo: two scan
pipelines (one per vertical) and one release build. Don't add a fourth
without a real, currently-running movement behind it.

Copy `../_templates/process.md` for a new card.
